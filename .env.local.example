# Required for the skill test/sharpen API routes (POST /api/skills/test, POST /api/skills/sharpen).
# Get a key at https://console.anthropic.com → Settings → API Keys.
# Without this, those routes return { ok: false, error: "missing_api_key" }.
ANTHROPIC_API_KEY=
